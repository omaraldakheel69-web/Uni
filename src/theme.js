import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2cd219ff",
    },
    background: {
      default: "#f4f6f8",
    },
  },
});

export default theme;